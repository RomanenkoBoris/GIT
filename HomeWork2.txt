Homewor 2
Student: Romanenko Boris

1) git checkout -f HEAD
   git checkout .

2) git clear -xdf

3) git rm --cached fileName
   git restore --staged fileName
   git reset fileName

4) git checkout HEAD~
   git reset HEAD~

5) git commit --amend -m "new commentar"

6) git revert HEAD

7) Т.к. при вводе команды git push в консоль будет выведена ошибка что локальный и удаленные репозитории не 
совпадают и нам будет предложено выполнить команду git pull.
